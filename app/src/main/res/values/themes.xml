<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- This is the styling for action bar -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="background">@color/my_backgrounds_color</item>
    </style>

    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="titleTextStyle">@style/MyActionBarTitle</item>
        <item name="background">@color/my_background_color</item>
    </style>

    <style name="MyActionBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#EEBA30</item>
    </style>
    <style name="asd" parent="@style/Widget.AppCompat.ActionButton">
        <item name="background">@color/cardview_light_background</item>
    </style>
</resources>